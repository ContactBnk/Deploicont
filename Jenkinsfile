pipeline {
    agent any
    stages {
        stage('Init') {
            steps {
                echo "Stage d'Init  - jenkinsfile"
            }
        }
        stage('Build') {
            steps {
                echo "Stage de Build  - jenkinsfile"
            }
        }
        stage('Deploy') {
          steps {
              echo "Stage Deploy  - jenkinsfile"
          }
        }
        
    }
}